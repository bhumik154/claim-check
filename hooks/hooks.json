{
  "description": "Verifies a test-count claim in a git commit message against a real pytest run before the commit is allowed through the Bash tool.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" claude_hook",
            "timeout": 150
          }
        ]
      }
    ]
  }
}
